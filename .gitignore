# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Node
node_modules/
dist/
build/
# Exception: cli/dist/ is shipped as the runtime artifact (PR #0 v1.4.2).
!cli/dist/
!cli/dist/**
*.tgz
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
*.swo

# Misc
*.tmp
*.bak
.cache/
coverage/

# Claude Code — local-only state
.claude/settings.local.json
.claude/worktrees/

# Rubrix — generated task state
.rubrix/

# Rubrix eval scaffold — not tracked in plugin repo
iteration-*/

# Rubrix per-run evidence — Release asset only, never committed
/reports/
reports/v*/

# Plugin root — not part of Claude Code plugin structure
# Anchored to repo root so cli/bin/ is not affected
/registry/
/bin/

# Codex companion job state
.codex/
