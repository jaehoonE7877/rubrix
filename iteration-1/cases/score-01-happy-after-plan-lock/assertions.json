{
  "case_id": "score-01-happy-after-plan-lock",
  "skill": "score",
  "assertions": [
    { "id": "a1", "text": "state is terminal Passed for the happy scoring case", "source": "rubrix.json", "check": "json_path_equals", "path": "$.state", "expected": "Passed" },
    { "id": "a2", "text": "scores has exactly one score per rubric criterion", "source": "rubrix.json", "check": "json_array_lengths_equal", "paths": ["$.scores", "$.rubric.criteria"] },
    { "id": "a3", "text": "each score includes criterion and score", "source": "rubrix.json", "check": "json_array_items_have_keys", "path": "$.scores", "keys": ["criterion", "score"] },
    { "id": "a4", "text": "each score criterion maps to an existing criterion id", "source": "rubrix.json", "check": "json_array_field_subset", "path": "$.scores", "field": "criterion", "allowed": ["c1", "c2", "c3"] },
    { "id": "a5", "text": "all 3 locks remain true (no regression)", "source": "rubrix.json", "check": "json_paths_equal", "paths": ["$.locks.rubric", "$.locks.matrix", "$.locks.plan"], "expected": true }
  ]
}
